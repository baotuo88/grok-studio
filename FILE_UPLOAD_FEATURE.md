# 聊天文件上传功能说明

## 功能概述

聊天界面现已支持上传文件（图片、文档等）进行分析和讨论。

## 支持的文件类型

- **图片**: PNG, JPG, JPEG, GIF, BMP, SVG, WEBP 等
- **文档**: PDF, DOC, DOCX, TXT, CSV, JSON 等

## 使用方法

### 1. 上传文件

在聊天输入框左侧，点击 📎 图标按钮，选择要上传的文件。

### 2. 文件限制

- 单个文件最大 10MB
- 最多同时上传 5 个文件
- 支持多选上传

### 3. 预览和管理

- 上传后会在输入框上方显示文件预览
- 图片文件显示缩略图
- 文档文件显示文件图标和名称
- 点击预览中的 ❌ 按钮可删除单个文件

### 4. 发送消息

- 可以只上传文件不输入文字（系统会自动添加"请分析这些文件"）
- 也可以同时输入文字和上传文件
- 点击发送按钮或按 Enter 键发送

## 技术实现

### 多模态消息格式

系统会将文件转换为 base64 格式，并构建符合 OpenAI Vision API 的多模态消息格式：

```javascript
{
  role: 'user',
  content: [
    { type: 'text', text: '用户输入的文字' },
    { type: 'image_url', image_url: { url: 'data:image/png;base64,...' } }
  ]
}
```

### 文件存储

- 文件以 base64 格式存储在本地 IndexedDB 数据库中
- 切换会话时会自动加载历史消息中的文件
- 文件数据与消息一起保存，确保历史记录完整

## 注意事项

1. **模型支持**: 确保使用的 AI 模型支持视觉/多模态功能（如 grok-vision-beta）
2. **文件大小**: 大文件会增加 API 请求大小和响应时间
3. **隐私安全**: 文件仅存储在本地浏览器，不会上传到第三方服务器
4. **浏览器兼容**: 需要现代浏览器支持 FileReader API 和 IndexedDB

## 界面更新

- 输入框左侧新增文件上传按钮
- 输入框上方新增文件预览区域
- 消息气泡中会显示附带的文件
- 提示文字更新为"支持上传图片和文档"

## 快捷键

- **Enter**: 发送消息
- **Shift + Enter**: 换行
- **Cmd/Ctrl + Enter**: 快速发送
